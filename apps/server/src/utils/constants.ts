/**
 * Shared server constants — single source of truth.
 */

/** Default tenant UUID used until multi-tenancy lands (auth/setup/routes all share this). */
export const DEFAULT_TENANT = '00000000-0000-0000-0000-000000000001';
